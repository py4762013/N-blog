<%- include header %>
<% posts.forEach(function(post, index) { %>
    <p>
        <h2>
            <!--<a href="/u/<%= post.name %>/<%= post.time.day %>/<%= post.title  %>"><%= post.title %>-->
            <a href="/p/<%= post._id %>"><%= post.title %></a>
        </h2>
    </p>
    <p class="info">
        Author: <a href="/u/<%= post.name %>"><%= post.name %></a> |
        Date: <%= post.time.minute %>
        <% if(post.reprint_info.reprint_from) { %>
            <br><a href="/u/<%= post.reprint_info.reprint_from.name %>/<%= post.reprint_info.reprint_from.day %>/<%= post.reprint_info.reprint_from.title %>">Description link</a>
        <% } %>
    </p>
    <p><%- post.post %></p>
    <p class="info">Read: <%= post.pv %> | Comments: <%= post.comments.length %></p>
<% }) %>
<%- include paging %>
<%- include footer %>
